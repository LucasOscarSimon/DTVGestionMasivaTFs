import { CabeceraBorradoMasivo } from '../../models/borrado-masivo/cabecera-borrado-masivo';
import { DetalleBorradoMasivo } from '../../models/borrado-masivo/detalle-borrado-masivo';
export class BorradoMasivo {
    obeCabecera: CabeceraBorradoMasivo;
    lobeDetalle: DetalleBorradoMasivo[];
}
